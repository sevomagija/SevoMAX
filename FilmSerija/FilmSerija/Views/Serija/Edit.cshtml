@model FilmSerija.Models.Serija

@{
    ViewBag.Title = "Izmeni seriju";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Izmeni seriju</h2>

@using (Html.BeginForm("Edit", "Serija", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.ValidationSummary(true)

    @Html.HiddenFor(m => m.Id)

    <div>
        @Html.LabelFor(m => m.Naslov)
        @Html.TextBoxFor(m => m.Naslov)
    </div>

    <div>
        @Html.LabelFor(m => m.Zanr)
        @Html.TextBoxFor(m => m.Zanr)
    </div>

    <div>
        @Html.LabelFor(m => m.Godina)
        @Html.TextBoxFor(m => m.Godina)
    </div>

    <div>
        @Html.LabelFor(m => m.BrojSezona)
        @Html.TextBoxFor(m => m.BrojSezona)
    </div>

    <div>
        <label>Nova slika</label>
        <input type="file" name="slika" />
        @if (!string.IsNullOrEmpty(Model.PutanjaSlike))
        {
            <p>Trenutna slika:</p>
            <img src="@Model.PutanjaSlike" width="100" />
        }
    </div>

    <button type="submit">Sačuvaj izmene</button>
}
